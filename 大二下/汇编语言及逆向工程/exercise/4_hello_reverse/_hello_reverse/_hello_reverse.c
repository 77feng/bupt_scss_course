#include<windows.h>  // "windows.h" 是 C/C++ 中仅适用于 Windows系统平台的头文件，
                     // 包含所有 Windows API中函数的声明、程序员常用的Windows宏
                     // 和各种函数所要使用到的数据类型和子系统

int main(){
	MessageBox(NULL, "Welcome to the world of reverse!", "Hi BYR ^_^", MB_OK);

	return 0;
}
